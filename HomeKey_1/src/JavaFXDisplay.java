/*

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Label;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;

public class JavaFXDisplay extends Application {

    private String formattedTable;

    public JavaFXDisplay(String formattedTable) {
        this.formattedTable = formattedTable;
    }

    @Override
    public void start(Stage primaryStage) {
        primaryStage.setTitle("Real Estate Data");

        // Create a label to display the formatted table
        Label tableLabel = new Label(formattedTable);

        // Create a VBox layout and add the label to it
        VBox root = new VBox(tableLabel);

        // Create the scene with the layout
        Scene scene = new Scene(root, 800, 600);

        // Set the scene for the primary stage
        primaryStage.setScene(scene);

        // Show the primary stage
        primaryStage.show();
    }
}
*/

